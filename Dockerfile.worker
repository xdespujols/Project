# BullMQ worker image (shared source, separate entrypoint)
FROM node:22-alpine
WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm ci --frozen-lockfile

COPY . .

ENV NODE_ENV=production

CMD ["npx", "tsx", "workers/start.ts"]
